/*
package wrapper.binarySearch;

public class RotatedArray {

    public static void main(wrapper.String[] args) {
        // Let us search 3 in below array
        int arr1[] = {5, 6, 7, 8, 9, 10, 1, 2, 3};
        int n = arr1.length;
        int key = 3;
        System.out.println("Index of the element is : "
                + pivotedBinarySearch(arr1, 0, n - 1, key));
    }

    private static wrapper.String pivotedBinarySearch(int[] arr, int start, int end, int key) {
        int pivot = findPivot(arr, start, end, key);
    }

    private static int findPivot(int[] arr, int start, int end, int key) {

        while (start <= end) {
            int mid = start + (end - start) / 2;

            if (mid <)


        }


    }
}
*/
